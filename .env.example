# PostHog (EU Cloud). Copy to .env and set your project API key from PostHog → Project settings.
# Without PUBLIC_POSTHOG_KEY, analytics is skipped (safe for local dev).
PUBLIC_POSTHOG_KEY=
# Optional; default is EU ingestion host:
# PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com
